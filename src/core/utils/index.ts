import { deleteChars, selectChars } from './stringUtil'
import {
  sample,
  shuffle,
  uniq,
  permutation,
  repeatedPermutation,
  fillArray,
  fillArray2D,
} from './arrayUtil'
import { noop } from './funcUtil'

export {
  deleteChars,
  selectChars,
  sample,
  shuffle,
  uniq,
  permutation,
  repeatedPermutation,
  fillArray,
  fillArray2D,
  noop,
}
