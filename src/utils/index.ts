import { noop, fillArray, fillArray2D } from './msUtil'
import {
  deleteChars,
  selectChars,
  sample,
  uniq,
  permutation,
  repeatedPermutation,
} from './xxdleUtil'

export {
  noop,
  fillArray,
  fillArray2D,
  deleteChars,
  selectChars,
  sample,
  uniq,
  permutation,
  repeatedPermutation,
}
